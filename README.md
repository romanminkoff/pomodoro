# pomodoro
cmd> python -m pip install -e .<br/>
cmd> python -m pip install -r requirements.txt<br/>
<br/>
In [1]: import pomodoro<br/>
<br/>
In [2]: pomodoro.run?<br/>
Signature: pomodoro.run(every=45)<br/>
Docstring: Pop up a picture every X minutes. every defaults to 45.<br/>
<br/>
In [3]: pomodoro.run(1)<br/>
Scheduling refuel time for every 1 minutes.<br/>
 13%|нн<br/>
